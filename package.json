{
  "name": "wait-port",
  "version": "0.1.0",
  "description": "Utility to wait for a TCP port to open.",
  "main": "./src/wait-port.js",
  "scripts": {
    "lint": "eslint .",
    "test": "nyc --report-dir 'artifacts/coverage' -x 'src/**/*.spec.js' --reporter=html --reporter=lcovonly --reporter=text mocha -t 10000 src/**/*.spec.js",
    "debug": "mocha --inspect --debug-brk src/**/*.spec.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dwmkerr/wait-port.git"
  },
  "author": "Dave Kerr",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dwmkerr/wait-port/issues"
  },
  "homepage": "https://github.com/dwmkerr/wait-port#readme",
  "devDependencies": {
    "eslint": "^3.19.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.3.0",
    "nyc": "^10.3.2"
  }
=
